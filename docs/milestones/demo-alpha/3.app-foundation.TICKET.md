# App foundation

**Type:** Frontend infra, no screens with real data yet. Depends on ticket 2's grants; prerequisite for `SPEC.md`'s Phase 1 build.

**Goal:** Everything needed before a screen can render real data: tooling installed, the Supabase client and auth wired, the route skeleton in place.

## Decisions
- Styling is NativeWind (Tailwind utility classes via `className`), not plain `StyleSheet` + `theme.ts` — see `docs/technical/frontend.md`.
- Data fetching is TanStack Query; raw `supabase.from(...)` calls stay out of components.
- Auth guarding at the route-layout level is a UX convenience only, not a security boundary — RLS doesn't exist yet, accepted as a temporary trust call for one seeded syndicate per `SPEC.md`.

## Tasks
- [ ] Install TanStack Query, `@react-native-async-storage/async-storage`, NativeWind + Tailwind.
- [ ] Create `babel.config.js` / `metro.config.js` / `tailwind.config.js` / `global.css`.
- [ ] `src/lib/supabase.ts` singleton: `EXPO_PUBLIC_SUPABASE_URL`/`_ANON_KEY`, `auth: { autoRefreshToken: true, persistSession: true, detectSessionInUrl: true }`, platform-conditional storage adapter (async-storage on native, `localStorage` on web).
- [ ] Root `AuthProvider` (`onAuthStateChange` + initial `getSession()`), exposing `{ session, user, status }`.
- [ ] Splash gate: first navigation waits on `status !== 'loading'`, using the already-installed `expo-splash-screen`.
- [ ] `QueryClientProvider`; wire TanStack Query's `focusManager` to React Native `AppState`.
- [ ] Route skeleton per `SPEC.md`'s Screen/Route Map: root `_layout.tsx`, `+not-found.tsx`, `(auth)/_layout.tsx` + `login.tsx`, `(app)/_layout.tsx` + `index.tsx`, `order/_layout.tsx` + `index.tsx` + `[orderItemId].tsx` — `order/` screens as placeholders.
- [ ] Root `.env.app.local.example`.

## Verification
- `pnpm web`, log in as a seeded user, land on an empty `/order`, session survives reload.

Belongs to @docs/milestones/demo-alpha/SPEC.md.
